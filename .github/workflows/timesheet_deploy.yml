name: Timesheet Migration

on:
  workflow_dispatch:

jobs:
  deploy-and-run:

    runs-on: self-hosted

    env:
      SQL_SERVER: ${{ secrets.SQL_SERVER }}
      SSIS_FOLDER: Timesheet
      SSIS_PROJECT: Timesheet_Migration
      SQL_AGENT_JOB: Timesheet_ETL_Load

    steps:

    - name: Checkout Repo
      uses: actions/checkout@v4

    - name: Build SSIS Project
      shell: powershell
      run: |
        $msbuild = "C:\Program Files\Microsoft Visual Studio\2022\BuildTools\MSBuild\Current\Bin\MSBuild.exe"

        if (!(Test-Path $msbuild)) {
          throw "MSBuild not found. Install Visual Studio Build Tools with SSDT."
        }

        & $msbuild `
          "Automation_Project\Timesheet_Migration\Timesheet_Migration.dtproj" `
          /p:Configuration=Development `
          /p:Platform="Any CPU"

    - name: Validate ISPAC Exists
      shell: powershell
      run: |
        $ispac = Get-ChildItem `
          -Path "Automation_Project" `
          -Recurse `
          -Filter *.ispac |
          Select-Object -First 1

        if (!$ispac) {
          throw "ISPAC file was not generated."
        }

        Write-Host "ISPAC found: $($ispac.FullName)"

    - name: Create SSIS Folder If Missing
      shell: powershell
      run: |
        sqlcmd `
          -S $env:SQL_SERVER `
          -E `
          -C `
          -TrustServerCertificate `
          -d SSISDB `
          -Q "
          IF NOT EXISTS (
              SELECT 1
              FROM catalog.folders
              WHERE name = '$env:SSIS_FOLDER'
          )
          BEGIN
              EXEC catalog.create_folder
                  @folder_name = '$env:SSIS_FOLDER';
          END
          "

    - name: Deploy ISPAC to SSISDB
      shell: powershell
      run: |
        $ispac = Get-ChildItem `
          -Path "Automation_Project" `
          -Recurse `
          -Filter *.ispac |
          Select-Object -First 1

        $sql = @"
        DECLARE @ProjectBinary VARBINARY(MAX);

        SELECT @ProjectBinary = BulkColumn
        FROM OPENROWSET(
            BULK '$($ispac.FullName)',
            SINGLE_BLOB
        ) AS ProjectFile;

        EXEC catalog.deploy_project
            @folder_name = '$env:SSIS_FOLDER',
            @project_name = '$env:SSIS_PROJECT',
            @project_stream = @ProjectBinary;
        "@

        sqlcmd `
          -S $env:SQL_SERVER `
          -E `
          -C `
          -TrustServerCertificate `
          -d SSISDB `
          -Q $sql

    - name: Run SQL Server Agent Job
      shell: powershell
      run: |
        sqlcmd `
          -S $env:SQL_SERVER `
          -E `
          -C `
          -TrustServerCertificate `
          -d msdb `
          -Q "
          EXEC dbo.sp_start_job
              @job_name = '$env:SQL_AGENT_JOB';
          "

    - name: Validate Job Started
      shell: powershell
      run: |
        sqlcmd `
          -S $env:SQL_SERVER `
          -E `
          -C `
          -TrustServerCertificate `
          -d msdb `
          -Q "
          SELECT TOP 1
              j.name AS JobName,
              a.start_execution_date,
              a.stop_execution_date
          FROM dbo.sysjobs j
          INNER JOIN dbo.sysjobactivity a
              ON j.job_id = a.job_id
          WHERE j.name = '$env:SQL_AGENT_JOB'
          ORDER BY a.start_execution_date DESC;
          "
